import { PayHistory } from "../../types/history";

export type PayHistoryItemProps = {
  history: PayHistory;
};

export const PayHistoryItem = ({}: PayHistoryItemProps) => {
  return <div></div>;
};
